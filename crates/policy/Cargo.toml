[package]
name = "policy"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = { workspace = true }
toml = "0.8"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
tempfile = "3.8"
